# Exposi-o-catadores
git clone https://github.com/seu-usuario/exposicao-catadores.git cd exposicao-catadores # Coloque os 4 arquivos na pasta git add . git commit -m "Initial commit: Add HTML page and project images" git push -u origin main
